apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: searchengine
  labels:
    app: auth-service
spec:
  ports:
  - port: 8000
    name: http
    targetPort: 8000
  selector:
    app: auth